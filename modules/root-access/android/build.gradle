plugins {
    id 'expo-module-gradle-plugin'
}

group = 'expo.modules.rootaccess'
version = '1.0.0'

android {
    namespace 'expo.modules.rootaccess'

    defaultConfig {
        versionCode 1
        versionName "1.0.0"
    }
}
